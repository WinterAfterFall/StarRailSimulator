# `src/Declaration/Class/Class.h`

รวม forward declaration ของคลาสหลักที่ header ฝั่งฟังก์ชันอ้างถึง เช่น Unit, AllyUnit, CharUnit, Enemy, action data และ trigger ไม่มี implementation ในไฟล์นี้ ปัจจุบัน `CharUnit` ถูกประกาศซ้ำหนึ่งครั้ง (จดเก็บกวาดไว้ใน future-improvements.md)

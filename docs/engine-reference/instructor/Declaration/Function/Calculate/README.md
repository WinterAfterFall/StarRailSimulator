# Declaration / Function / Calculate

โฟลเดอร์ mirror ของ `src/Declaration/Function/Calculate/` ไฟล์ `Library.h` และ `include.h` ทำหน้าที่รวม header จึงไม่มีคู่มือรายไฟล์ตามกติกาของ instructor อ่าน `.md` ชื่อเดียวกับ source สำหรับรายละเอียดของไฟล์ที่มีเนื้อหา

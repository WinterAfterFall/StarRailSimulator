# `src/Declaration/Function/Setup/SetEnemy.h`

ประกาศ `createNewEnemy()` และ `SetupEnemy()` โดยพารามิเตอร์ชนิดศัตรูเป็น `EnemyType` ให้ตรงกับนิยามใน [SetEnemy.md](../../../Function/Setup/SetEnemy.md) จุดนี้เคยประกาศผิดเป็น `string` และแก้แล้ว (BUGS #20)

# `src/Defination/Function/Setup/SetCombat.h`

## `Reset()`

2026-09-18: ลบ debug output `cout << calculateCritdamOnStats(charUnit[1].get())` ซึ่งพิมพ์ Crit DMG ของตัวละครช่อง 1 ทุก simulation run โดยไม่มี label/newline และไม่มีผลต่อการคำนวณ

# `src/Defination/Function/AdjustFunction/EnemyCmd.h`

`EnemyCmd::setEnemyWeakness()` รับ flag ครบเจ็ดธาตุแล้วเขียน `Enemy_weak` ใหม่ทั้งชุด ค่านี้ถูก `SetupEnemy()` คัดลอกลงศัตรูที่สร้างภายหลัง จึงเป็นการกำหนดค่าเริ่มต้นของศัตรูใหม่ ไม่ได้ย้อนแก้ศัตรูที่สร้างไปแล้ว

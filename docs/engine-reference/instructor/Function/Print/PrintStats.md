# `src/Defination/Function/Print/PrintStats.h`

`AllyUnit::printHpStats()` พิมพ์ชื่อยูนิตและค่า HP ปัจจุบัน/รวม/ฐาน, HP% และ flat HP; `printCritStats()` พิมพ์ชื่อพร้อม Crit Rate และ Crit DMG ทั้งคู่เป็น helper สำหรับดูค่า stats ของยูนิตขณะรัน

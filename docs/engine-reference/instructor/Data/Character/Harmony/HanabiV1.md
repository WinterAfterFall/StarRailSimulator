# `src/Defination/Data/Character/Harmony/HanabiV1.h`

**เวอร์ชันเก่าของ Hanabi — ไม่ใช่ตัวที่ใช้งานจริง** · ตัวจริงคือ `Hanabi.h` (ดู `Hanabi.md`)

`docs/kit-reference/Character/README.md` ระบุว่า "ข้าม V1 dupes (`HanabiV1`, `Black SwanV1`) — ดูไฟล์ตัวจริงแทน"

> **แต่ยัง `#include` อยู่**: `All_Harmony_char.h:4` → `#include"HanabiV1.h"`

## สิ่งที่เคยแก้ในไฟล์นี้

`../../README.md` หัวข้อ "แก้เมื่อ 2026-09-13" บันทึกไว้ว่า:

> **Hanabi / HanabiV1** — เดิม `addUltCondition` เป็น `LC != "DDD" && ต้นเทิร์นของเป้าที่บัฟ` → **ใส่ DDD แล้วเงื่อนไขเป็นเท็จเสมอ = ไม่มีวันกดอัลติ** · ลบส่วน `LC != "DDD"` ออก → กดอัลติตอนต้นเทิร์นของเป้าที่บัฟเสมอ ไม่ว่าใส่ LC อะไร

การแก้ครั้งนั้นทำกับ **ทั้งสองไฟล์** เพราะยังคอมไพล์อยู่ทั้งคู่ — เป็นตัวอย่างที่ชัดว่าการเก็บไฟล์ V1 ไว้ทำให้ต้องแก้ซ้ำสองที่

## ควรทำยังไงต่อ

เหมือน `../Nihility/Black SwanV1.md` — เป็นโค้ดตายที่ยังถูกคอมไพล์และเป็นแหล่งความจริงที่สอง · ถ้าไม่ได้ใช้เทียบผลแล้วควรลบทั้งไฟล์และ `#include` **แต่ยังไม่ได้ถาม user จึงยังไม่แตะ**

# `src/Defination/Data/Lightcone/Destruction/Secret_Vow_Nobuff.h`

`namespace Destruction_Lightcone` · `Light_cone.Name` = `"Secret_Vow"` · base stats `SetAllyBaseStats(1058, 476, 265)`

ฟังก์ชันชื่อ `Secret_Vow_NoBuff`

| ท่อน | โค้ด |
|---|---|
| DMG `15 + 5S` | `Reset_List` |

**เวอร์ชัน "ไม่เข้าเงื่อนไข" ของ `Secret_Vow.h`** — ดูคำอธิบายเต็มที่ `Secret_Vow.md`

`Light_cone.Name` ถูกตั้งเป็น `"Secret_Vow"` เหมือนไฟล์หลัก ไม่ใช่ชื่อที่แยกได้

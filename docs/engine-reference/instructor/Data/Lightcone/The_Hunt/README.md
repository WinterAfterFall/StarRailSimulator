# `src/Defination/Data/Lightcone/The_Hunt/`

**ไม่มี Light Cone เลย** — มีแต่ `All_The_Hunt_LC.h`

โฟลเดอร์ชื่อ `The_Hunt` (มี `_`) ต่างจากฝั่งตัวละครที่ใช้ `The Hunt` (มีช่องว่าง)

## สิ่งที่ยังไม่มี

path The Hunt ในเกมมี Light Cone จำนวนมาก (signature ของ Seele / Feixiao / Dan Heng / Topaz / Boothill / Yanqing และใบ 4★ อีกหลายใบ) ซึ่งอยู่ในคิว `../../IMPLEMENT-QUEUE.md`

**`../../Character/The Hunt/Archer.h` ซึ่งเป็นตัวละครเดียวของ path นี้จึงยังไม่มี signature LC ของตัวเอง**

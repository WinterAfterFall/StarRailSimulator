# `src/Enum/Enum.h`

นิยาม enum หลักของ engine เช่น `AType`, `UnitStatus`, `ElementType`, `Stats`, `DotType` และชนิดแหล่งดาเมจ/ฮีลที่โค้ดส่วนอื่นใช้ ไฟล์นี้เก็บชนิดข้อมูล ไม่ใช่ implementation ของสูตรหรือวงจรต่อสู้

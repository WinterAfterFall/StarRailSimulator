# `src/Enum/CheckMode.h`

นิยาม `DmgFormulaMode` ที่ใช้เลือกระดับรายละเอียดการตรวจสูตรดาเมจ รวม `All`, กลุ่มค่าและตัวคูณแต่ละชนิด เช่น `Crit`, `DefShred`, `Merrymake`, `CB` ไฟล์นี้เก็บชนิดข้อมูล ไม่ใช่ implementation ของสูตรหรือวงจรต่อสู้

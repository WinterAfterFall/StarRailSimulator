# `src/Enum/StatusEnum.h`

นิยามโหมด runtime ได้แก่ `DriverType`, `SPMode`, `PhaseStatus` และ `SubstatsRerollMode`; ปัจจุบันโหมด reroll ที่เปิดใช้งานคือ `Standard` ไฟล์นี้เก็บชนิดข้อมูล ไม่ใช่ implementation ของสูตรหรือวงจรต่อสู้

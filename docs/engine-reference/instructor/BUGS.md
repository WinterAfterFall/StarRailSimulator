# 🐞 BUGS — บั๊กที่เจอระหว่างไล่โค้ด

เลขข้อคงไว้ตามเดิมเพราะมีเอกสารอื่นอ้างถึง · **#15 ไม่มี** (เลขถูกเลื่อนตอนแทรก #16 เมื่อ 2026-09-09)

| # | สถานะ | เรื่อง | โค้ด | อธิบายที่ |
|---|---|---|---|---|
| 1 | ✅ 2026-09-13 | `#include "AllyUnit.h"` ซ้ำ | `Class/Unit/Library.h` | [Class/Unit/README.md](Class/Unit/README.md) |
| 2 | ✅ 2026-09-13 | summon ไม่ได้ชื่อของตัวเอง (Jingyuan → LL) | `Class/Unit/StatsSet.h` | — |
| 3 | ✅ 2026-09-02 | comment `AtvFreeze` / `Retire` ผิด | `src/Enum/Enum.h` | [Class/Unit/Unit.md](Class/Unit/Unit.md) |
| 4 | ✅ 2026-09-13 | `isExsited` สะกดผิด | `Class/Unit/Unit.h` | [Class/Unit/Unit.md](Class/Unit/Unit.md) |
| 5 | ✅ 2026-09-02 | `decreaseSheild()` คืนดาเมจทะลุโล่ผิด | `Function/Combat/ChangeHP.h` | [Function/Combat/ChangeHP.md](Function/Combat/ChangeHP.md) |
| 6 | ✅ 2026-09-04 | taunt increase% ใช้งานไม่ได้ | `Class/Unit/AllyUnit.h` | [Class/ActionData/EnemyActionData.md](Class/ActionData/EnemyActionData.md) |
| 7 | ✅ `8690113` | `currentMemoNum` ไม่ reset | `Function/Setup/Stats_Reset.h` | [Class/Unit/AllyUnit.md](Class/Unit/AllyUnit.md) |
| 8 | ℹ️ ปิด ไม่แก้ | summon / countdown `owner == nullptr` | `Class/Unit/StatsSet.h` | [Class/Unit/AllyUnit.md](Class/Unit/AllyUnit.md) |
| 9 | ✅ 2026-09-02 | `HpAdjust` หัก currentHP เกินตอนถอนบัฟ maxHP | `Function/Combat/AdjustStats.h` | [Class/Unit/AllyUnit.md](Class/Unit/AllyUnit.md) |
| 10 | ✅ 2026-09-02 | `Turn_priority` ไม่ reset | `src/Setting.h` · `Function/Setup/SetCombat.h` | [Function/Combat/Action_value.md](Function/Combat/Action_value.md) |
| 11 | ✅ 2026-09-02 | enemy single-target สร้างความเสียหาย 0 (`UnitGotHit`) | `Class/ActionData/EnemyActionData.h` | [Class/ActionData/EnemyActionData.md](Class/ActionData/EnemyActionData.md) |
| 12 | ✅ 2026-09-04 (บางส่วนปล่อยไว้) | dead code โซน taunt | หลายไฟล์ | [Class/ActionData/EnemyActionData.md](Class/ActionData/EnemyActionData.md) |
| 13 | ✅ 2026-09-02 | `addTaunt` ไม่ dedup + Mydei ไม่ออกจาก taunt list | `Function/Combat/EnemyCombat.h` · `Data/Character/Destruction/Mydei.h` | [Class/ActionData/EnemyActionData.md](Class/ActionData/EnemyActionData.md) |
| 14 | ✅ 2026-09-02 | `DecreaseHP` ทั้งทีม `return` แทน `continue` | `Function/Combat/ChangeHP.h` | [Function/Combat/ChangeHP.md](Function/Combat/ChangeHP.md) |
| 16 | ✅ 2026-09-13 | `Cal_AverageDamage` ใช้ตัวคูณเฉลี่ยของ `recv` | `Function/Calculate/CalDamageNote.h` | [Function/Calculate/CalDamageNote.md](Function/Calculate/CalDamageNote.md) |
| 17 | ℹ️ by design | ตัวละครตายจากดาเมจไม่ได้ | `Function/Combat/ChangeHP.h` | [Function/Combat/ChangeHP.md](Function/Combat/ChangeHP.md) |
| 18 | ✅ 2026-09-13 | Certified Banger อ่าน `Merrymake` แทน `CertifiedBanger` | `Function/Calculate/CalStats.h` | [Function/AdjustFunction/FormulaCheck.md](Function/AdjustFunction/FormulaCheck.md) |
| 19 | ✅ 2026-09-16 | Castorice สร้าง Netherwing ก่อนค่าฐาน + LC | `Data/Character/Remembrance/Castorice.h` | [Class/Unit/StatsSet.md](Class/Unit/StatsSet.md) |
| 20 | ✅ 2026-09-21 | declaration ของ `SetupEnemy` ใช้ `string` แต่ implementation ใช้ `EnemyType` | `Declaration/Function/Setup/SetEnemy.h` | [Function/Setup/SetEnemy.md](Function/Setup/SetEnemy.md) |
| 21 | ✅ 2026-09-21 | อ่าน `breakDotList` iterator หลัง `erase()` ใน after-turn event | `Function/Event/Event.h` | [Function/Event/Event.md](Function/Event/Event.md) |
| 22 | ✅ 2026-09-21 | `calDebuffStack()` นับสถานะ 0 stack และไม่ถอนจำนวนสถานะเมื่อ stack หมด | `Function/Combat/DebuffStack.h` | [Function/Combat/DebuffStack.md](Function/Combat/DebuffStack.md) |
| 23 | ✅ 2026-09-21 | `dotSingleStack()` นับ DoT ที่มี 0 stack | `Function/Combat/Dot.h` | [Function/Combat/Dot.md](Function/Combat/Dot.md) |
| 24 | ✅ 2026-09-21 | Arcana 1 stack ถูกหารเหลือ 0 หลัง DoT tick | `Data/Character/Nihility/Black Swan.h` | [Data/Character/Nihility/Black Swan.md](Data/Character/Nihility/Black%20Swan.md) |
| 25 | ✅ 2026-09-22 | สูตร CR/CD เขียนซ้ำสองที่ — แก้ป้าย debug ไปโดนชุดที่ไม่มีผู้เรียก | `Function/Calculate/CalStats.h` | [Function/Calculate/CalStats.md](Function/Calculate/CalStats.md) |

## รายละเอียด

1. ~~`Unit/Library.h` — `#include "AllyUnit.h"` ซ้ำ 2 บรรทัด~~ ✅ แก้แล้ว (2026-09-13) — ลบบรรทัดที่ 8 ออก
2. ~~**Jingyuan summon ไม่มีชื่อของตัวเอง**~~ ✅ แก้แล้ว (2026-09-13) — `StatsSet.h:74` เปลี่ยนเป็น `Atv_stats->Name = Name;` (แบบเดียวกับ `SetCountdownStats`) · ⚠️ **เปลี่ยนผล sim ได้**: `Buff_Stats.h:22` ตัดสินว่าบัฟหมดอายุด้วย `turn->Name == ชื่อคนถือบัฟ` — เดิมเทิร์นของ LL ชื่อ "Jingyuan" จึงถูกนับเป็นเทิร์นของ Jingyuan ด้วย · หลังแก้ไม่นับแล้ว · โค้ด Jingyuan เองเช็คเทิร์นด้วย `num` + `side` (`Jingyuan.h:66`) ไม่กระทบ · log เทิร์นจะขึ้นว่า `LL` · บันทึกเดิม: — `Jingyuan.h:108` `SetSummonStats(ptr, 60, "LL")` ส่งชื่อ `"LL"` แต่ `StatsSet.h:74` เขียน `Atv_stats->Name = ptr->Atv_stats->Name` → argument `Name` ถูกทิ้ง summon เลยชื่อ `"Jingyuan"`. ตรวจว่าปัจจุบันมีจุดไหน lookup ชื่อ summon แยกจากเจ้าของหรือไม่ ก่อนตัดสินใจแก้
3. ~~`Enum.h:30-31` comment stale~~ ✅ แก้แล้ว (2026-09-02) — `AtvFreeze`/`Retire` = ใช้ตอนอัลติ Phainon, atv แช่แข็งทั้งคู่
4. ~~`Unit::isExsited()` — typo~~ ✅ แก้แล้ว (2026-09-13) — rename เป็น `isExisted` ทั้ง 9 จุด (`Unit.h` · `AllyAttackAction.h` ×3 · `AllyBuffAction.h` · `Arcadia.h` · `ChangeHP.h` ×2 · `Energy.h`)
5. ~~`decreaseSheild()` บั๊ก~~ ✅ แก้แล้ว (2026-09-02, `ChangeHP.h:175`) — ดู [ChangeHP.md](Function/Combat/ChangeHP.md) (ระบบโล่) · ระบบ **สร้าง** โล่ยัง stub (ยังไม่แก้)
6. ~~"taunt increase%" stub เสีย~~ ✅ แก้แล้ว — 2026-09-04 rework: field `tauntIncrease` (% เพิ่ม, 0 = ไม่มี), `taunt = baseTaunt · (1 + tauntIncrease/100)`, method `tauntIncreaseChange(double)` (`AllyUnit.h:29,62` · reset `= 0` ที่ `Stats_Reset.h:36,264`) · ยังไม่มี caller → ดู #12
7. ~~**`currentMemoNum` ไม่เคย reset**~~ ✅ แก้แล้ว (commit `8690113` 2026-09-04 · โค้ดตอนนี้ `Stats_Reset.h:35,260` เขียน `= defaultMemoNum` · ตรวจซ้ำ 2026-09-13 — รายการเดิมลืมอัปเดตสถานะ) · บันทึกเดิม: `Stats_Reset.h:35` + `:260` `currentMemoNum = currentMemoNum` (self-assign · ควร `= defaultMemoNum`) → ถ้าค้าง ≥ 1: `chooseSubUnitBuff` (`TargetChoose.h:17`) อ่าน `memospriteList[currentMemoNum]` out-of-bounds. ยังไม่ crash = ยังไม่มีตัวละครไหน set `currentMemoNum` ≥ 1 (ณ 2026-09-02) · _รอ user ยืนยันก่อนแก้_
8. ~~**summon/countdown `owner == nullptr`**~~ ℹ️ **ปิด ไม่แก้** (user 2026-09-13: summon/countdown ไม่ได้ใช้ `owner`) · บันทึกเดิม: capture `[ptr]` ใน lambda ทำให้โค้ด summon/countdown เองไม่พึ่ง `this->owner` · **แต่** `buffSingle` path บัฟ speed (`Buff_Stats.h:88,99`) deref `ptr->owner->path` → latent crash ถ้ามีใคร cast summon → `AllyUnit*` แล้ว speed-buff (ยังไม่มีใครทำ) · **แผน:** refactor summon/countdown → `ActionValueStats` ล้วน (ดู `future-improvements.md` #3) — ไม่ใช่ปะ null-guard
9. ~~`HpAdjust` หัก currentHP เกินตอนถอนบัฟ maxHP~~ ✅ แก้แล้ว (2026-09-02, `AdjustStats.h:12`) — ดู [AllyUnit.md](Class/Unit/AllyUnit.md) · เดิม: `maxHP` ลดแม้ currentHP ยังไม่ชนเพดานใหม่ → currentHP โดนหักตาม delta
10. ~~`Turn_priority` global ไม่ reset~~ ✅ แก้แล้ว (2026-09-02) — rename → `nextForwardPriority` (`Setting.h:87`) + reset **ต่อ run** ที่ `Reset()` (`SetCombat.h`)
11. ~~`UnitGotHit` ไม่เคยถูก populate → enemy single-target attack สร้างความเสียหาย 0~~ ✅ แก้แล้ว (2026-09-02, `EnemyActionData.h:47,72`) — เพิ่ม `UnitGotHit.push_back(each);` ข้าง `Increase_energy` ทั้ง 2 branch · เดิม: `setBaAttack` เดิน round-robin + ให้ energy แต่ `UnitGotHit` ว่างตลอด → damage loop ไม่วน → single-target สร้างความเสียหาย 0 (AoE ปกติ) · push ในลูป = โดนพร้อมกันหลายตัวได้ = ปกติตามดีไซน์ ([EnemyActionData.md](Class/ActionData/EnemyActionData.md))
12. **dead code โซน taunt** — ✅ ลบแล้ว (2026-09-04): no-arg `calHitChance()` · `totalTaunt` (global + `SetCombat.h` 3 จุด) · `Enemy::removeTaunt(string)` · `totalTaunt` bookkeeping ใน `tauntMtprChange` · **ปรับ:** `tauntMtpr` (100 = ×1.0) → `tauntIncrease` (0 = ไม่มี, `taunt = baseTaunt · (1 + tauntIncrease/100)`), method → `tauntIncreaseChange(double)` (2026-09-04) · **ยังไม่ลบ** (โซนอื่น): `DecreaseHP(Unit*, vector<AllyUnit*>, ...)` (`ChangeHP.h:149`) · **แก้ข้อสรุป 2026-09-16:** `Enemy::hitCount` มีการใช้งานตามที่ user ยืนยัน ไม่จัดเป็น dead code; ตรวจพบ reset และ `++` ใน `Attack()` ส่วนรายละเอียดการอ่านค่าฝั่ง Enemy ยังไม่ได้ไล่ครบ (ฝั่ง `AllyUnit::hitCount` อ่านโดย `Grand_Duke`)
13. ~~`Enemy::addTaunt` ไม่มี dedup + Mydei ไม่ออกจาก taunt list~~ ✅ แก้แล้ว (2026-09-02) — `addTaunt` เช็คชื่อก่อน push (`EnemyCombat.h:13`) · Mydei ult `debuffApply(...,"Mydei_Taunt",2)` + `addTaunt` · `After_turn_List` `isDebuffEnd(e,...)` → `e->removeTaunt(Mydei)` (แยกอิสระต่อ enemy) — ดู [EnemyActionData.md](Class/ActionData/EnemyActionData.md)
14. ~~`DecreaseHP` ทั้งทีม `return` แทน `continue`~~ ✅ แก้แล้ว (2026-09-02, `ChangeHP.h:141,153,167`) — เดิม: ใน loop วน `allyList` เจอ ally ที่ `!isTargetable()` (เช่น memo ยังไม่ summon) → `return` ออกทั้งฟังก์ชัน → ตัวที่เหลือไม่โดนลดเลือด · กระทบ `Hyacnine_LC.h:28` (ลดเลือดทั้งทีม) · single-target overload (`:128`) `return` ถูกแล้ว (ไม่มี loop)
16. ~~**True DMG ที่เป็น non-real-time ใช้ `toughnessAvgMultiplier` ของ `recv` แทน `src`**~~ ✅ แก้แล้ว (2026-09-13, `CalDamageNote.h` `Cal_AverageDamage`) — อัปเดตตัวคูณเฉลี่ยให้ศัตรู **ทุกตัว** ก่อนวน แล้วใช้ `each.first.src->toughnessAvgMultiplier` · **กระทบเฉพาะ True DMG ที่ย้ายเป้า** (Tribbie E1 · Phainon E6 · Cipher) เพราะดาเมจปกติส่ง `src == recv` (`CalDamage.h` ทั้ง 6 จุด) และ RMC Mem's Support ส่ง `src, src` · เลขบรรทัดในหมายเหตุด้านล่างเป็นของก่อนแก้ · บันทึกเดิม: — `Cal_AverageDamage` (`CalDamageNote.h:44-46`) วน `currentNonRealTimeDmg` แล้วคูณด้วย `enemy->toughnessAvgMultiplier` โดย `enemy` = ตัวที่ตรงกับ **`each.first.recv`** :
    ```cpp
    for(auto &each : ptr->currentNonRealTimeDmg){
        if(each.first.recv->getNum() != enemy->getNum())continue;
        rec += each.second.total * enemy->toughnessAvgMultiplier;   // <- ของ recv
    }
    ```
    แต่เจตนาของ field `src` (ดู [CharUnit.md](Class/Unit/CharUnit.md)) คือ "weaken/toughness-avg อิงจากศัตรูที่ดาเมจ **ต้นทาง** ลง" → บรรทัดนี้ควรใช้ `toughnessAvgMultiplier` ของ `each.first.src`
    - **เห็นผลเมื่อ** note ตกสมุด non-real-time **และ** `src != recv`
    - ⚠️ **นี่คือเคสหลัก ไม่ใช่เคสหายาก** — สมุด non-real-time คือสมุดของการโจมตี **ปกติทั้งหมด** (BA/Skill/Ult/FuA/DoT/Additional/Elation ดู [CalDamageNote.md](Function/Calculate/CalDamageNote.md)) ดังนั้น **Tribbie E1** ที่งอก True DMG จากการโจมตีปกติ แล้วย้ายยอดไปกองเป้าเดียว → เข้าเงื่อนไขเต็ม ๆ ทุกครั้ง
    - ที่ **ไม่** ต่างคือตอน `src == recv` (True DMG ที่ไม่ย้ายเป้า) หรือ note ตกสมุด real-time (Break/SPB/Technique/Freeze/Entanglement — ไม่คูณ multiplier ตอนจบเลย)
    - **ผิดแค่ครึ่งเดียว** (พบ 2026-09-13): `Cal_DamageSummary` (`CalDamageNote.h`) ที่รวม **total** คูณด้วย `each.first.src->toughnessAvgMultiplier` = **ถูกแล้ว** · ตัวที่ใช้ `recv` คือ `Cal_AverageDamage` ซึ่งทำ **ดาเมจเฉลี่ยต่อ ATV** (`AvgDmgRecord`) — และ optimizer substats (`changeMaxDamage`) เทียบด้วยค่าเฉลี่ยตัวนี้ → บั๊กนี้กระทบ **การเลือก substats** ด้วย ไม่ใช่แค่ตัวเลขที่ปรินต์ (ดู [Substats_Reset.md](Function/Setup/Substats_Reset.md))
    - **บรรทัดที่ผิด:** `CalDamageNote.h:46` — `rec += each.second.total*enemy->toughnessAvgMultiplier;` โดย `enemy` คือตัวที่ตรงกับ `recv` (กรองไว้ที่บรรทัด 45)
    - **ต้องแก้ 2 จุด ไม่ใช่จุดเดียว:** (1) บรรทัด 46 เปลี่ยนเป็น `each.first.src->toughnessAvgMultiplier` · (2) บรรทัด 38 อัปเดตตัวคูณเฉลี่ยให้เฉพาะศัตรูใน `enemyList` (= `attackAction->targetList` ที่ส่งมาจาก `Combat.h:153`) ถ้า `src` ไม่ได้โดนตีในการโจมตีครั้งนี้ ค่าของมันจะค้างเก่า (หรือเป็น 0 ต้น run จาก `Stats_Reset.h:135`) → ต้องอัปเดตตัวคูณให้ศัตรู **ทุกตัว** ก่อนวนลูป เหมือนที่ `Cal_DamageSummary` ทำอยู่แล้ว (`CalDamageNote.h:84`)
    - _พบ 2026-09-09 ระหว่างทัวร์ `CharUnit.h` · แก้คำอธิบายที่กลับข้าง 2026-09-13 (เดิมเขียนว่า Break/SPB/DoT ตกสมุดเฉลี่ย — ผิด) · ยังไม่แก้โค้ด_
17. ℹ️ **ตัวละครตายจากดาเมจไม่ได้ = by design** (ยืนยัน 2026-09-04) — sim นี้วัด damage output ไม่แคร์ survivability · `DecreaseCurrentHP` (`ChangeHP.h:122`) clamp `currentHP` ขั้นต่ำ `1` โดยตั้งใจ · `AllyUnit::death()` ถูกเรียกแค่กับ summon/countdown/memo (FireFly/Phainon/Aglaea/Castorice/Robin) — char-death `AllyDeath_List` (Huohuo revive, Tingyun buff strip) จึงยิงเฉพาะตอน memo ตาย ไม่เคยยิงตอน char ตาย (ยอมรับได้)
18. ~~**`calPunchLineMultiplier` อ่าน `Merrymake` แทน `CertifiedBanger` ฝั่งเป้าหมาย**~~ ✅ แก้แล้ว (2026-09-13, `CalStats.h:379-386`) — copy-paste หลุดมาจาก `calMerryMakeMultiplier` ที่อยู่เหนือขึ้นไป 20 บรรทัด · ผิด 4 จุด: ฝั่ง `target` ในสูตร 2 จุด (`[AType::None]` + ในลูป `damageTypeList`) และบรรทัด `cout` ที่ป้ายเขียน "CB" แต่ดึงค่า `Merrymake` มาโชว์ทั้ง 2 ฝั่ง · **ตอนแก้ยังไม่เคยระเบิด** เพราะไม่มีโค้ดไหนแปะ `Merrymake`/`CertifiedBanger` ใส่ศัตรูเลย (ทั้งคู่เป็นบัฟฝั่ง ally) → ฝั่ง target บวก 0 เสมอ · แต่ถ้าอนาคตมีตัวที่ debuff ศัตรูด้วย CB มันจะถูกมองข้าม และ Merrymake ฝั่งศัตรูจะรั่วเข้าสูตร CB ผิด ๆ · ยืนยันกับ user 2026-09-13 ว่าฝั่งเป้าหมายต้องเป็น `CertifiedBanger` · คอมไพล์ผ่าน (`g++ -std=c++17 -fsyntax-only Application.cpp`)
19. ~~**Castorice เรียก `SetMemoStats` ก่อน `SetAllyBaseStats` และ `LC`**~~ ✅ แก้แล้ว (2026-09-16, `55713b9`) — `SetMemoStats` ถ่าย `baseAtk` / `baseDef` / `baseHp` ของเจ้าของครั้งเดียวตอนเรียก (`StatsSet.h:42-44`) และรีเซ็ตต่อ run ไม่คำนวณใหม่ · Netherwing จึงได้ `baseAtk = 0`, `baseDef = 0` · user ยืนยันว่าลำดับเดิมเป็นความบังเอิญ · ย้ายให้เป็น base → LC → Relic → Planar → `SetMemoStats` แบบ Aglaea / Hyacine / RMC · ตรวจแล้วว่า LC / Relic / Planar ที่อ้าง `memospriteList` อ่านใน lambda ทั้งหมด จึงย้ายได้ปลอดภัย · ผลเชิงตัวเลขน่าจะไม่มี (HP Netherwing คงที่ 34000, สกิลสเกลจาก HP ของ Castorice) แต่ยังไม่ได้รัน sim ยืนยัน
20. ~~**declaration ของ `createNewEnemy` / `SetupEnemy` รับ `string type` แต่ definition รับ `EnemyType`**~~ ✅ แก้แล้ว (2026-09-21) — เปลี่ยนสอง declaration ให้เป็น `EnemyType` ตรงกับ implementation และ caller; compile regression test ล้มก่อนแก้ด้วย invalid function-pointer conversion และผ่านหลังแก้
21. ~~**Break DoT expire อ่าน iterator หลัง erase**~~ ✅ แก้แล้ว (2026-09-21) — `Event.h` ใน `allEventAfterTurn()` เดิมลบสมาชิก `breakDotList` แล้วใช้ iterator ที่คืนจาก `erase()` อ่าน `type` ทั้งที่ชี้ไปสมาชิกถัดไปหรือ `end()` จึงเก็บชนิดก่อนลบและใช้ค่านั้นลดตัวนับ มี regression test ที่ล้มก่อนแก้เมื่อ Burn หมดอายุแต่ Shock ยังอยู่
22. ~~**`calDebuffStack()` นับสถานะ 0 stack**~~ ✅ แก้แล้ว (2026-09-21) — เดิมเพิ่ม `Total_debuff` ทันทีเมื่อ stack เดิมเป็น 0 แม้ stack ใหม่ยังเป็น 0 และไม่ลดจำนวนสถานะเมื่อ stack ลดจากบวกเป็น 0; user ยืนยันว่า 0 stack ไม่เป็นสถานะ จึงย้ายการนับไปตามการเปลี่ยนผ่าน 0 ↔ บวก มี regression test ล้มก่อนแก้ในกรณีเพิ่ม 0 stack
23. ~~**`dotSingleStack()` นับ DoT ที่มี 0 stack**~~ ✅ แก้แล้ว (2026-09-21) — เดิมเพิ่ม `DotCount` และตัวนับชนิด DoT เมื่อ stack เดิมเป็น 0 แม้คำขอเพิ่ม 0 หรือติดลบ และไม่ลดเมื่อ stack สุดท้ายหมด; user ยืนยันว่า 0 stack ไม่ควรนับ จึงใช้การเปลี่ยนผ่าน 0 ↔ บวก และคงการอัปเดตตัวนับก่อน `AfterApplyDebuff` มี regression test ล้มก่อนแก้ในกรณีเพิ่ม 0 stack
24. ~~**Arcana 1 stack ถูกหารเหลือ 0**~~ ✅ แก้แล้ว (2026-09-21) — หลัง DoT tick เวอร์ชันปัจจุบันใช้ `stack / 2` ตรง ๆ; user ยืนยันว่าต้องคงขั้นต่ำ 1 จึงใช้ `arcanaStacksAfterTick()` เฉพาะทางลด stack หลัง tick พร้อม regression test ขอบเขต 1–5 stack; `Black SwanV1.h` คง 1 stack อยู่แล้ว
25. ~~**สูตร CR/CD เขียนซ้ำสองที่ใน `CalStats.h`**~~ ✅ แก้แล้ว (2026-09-22) — `Cal_Crit_rate_multiplier()` / `Cal_Crit_dam_multiplier()` รวมค่า CR/CD จาก `Attacker` + `target` ตาม `damageTypeList` แต่ **ไม่มีผู้เรียกเลยทั้งเรโป** ขณะที่ `calCritMultiplier()` เขียนสูตรชุดเดียวกันซ้ำไว้ในตัวเอง ผลคือการแก้ป้าย debug เมื่อ 2026-09-20 (ช่อง `Base Crit dam` ที่เดิมแสดง `Stats::CR`) ไปโดนเฉพาะชุดที่ไม่ทำงาน ส่วนชุดที่ทำงานจริงไม่ถูกแก้ · แก้โดยให้ `calCritMultiplier()` เรียกสองฟังก์ชันนั้นแทนการคำนวณเอง เหลือสูตรชุดเดียว และปรับความกว้างคอลัมน์ debug ของทั้งสอง (`setw(6)` → `setw(7)` พร้อมเว้นวรรคป้าย) ให้ตรงกับที่เคยพิมพ์ ผลบนจอจึงไม่เปลี่ยน · ⚠️ **ต่างจากเดิมหนึ่งกรณี**: CR และ CD ติดลบพร้อมกัน เดิมได้ลบ×ลบเป็นบวก คืนตัวคูณเกิน 1 (เช่น CR −50, CD −100 → `1.5`) ตอนนี้ helper clamp ทั้งคู่เป็น 0 ก่อน จึงคืน `1.0` ตามเจตนาของ clamp ขั้นต่ำ กรณีอื่นเท่าเดิมทุกประการ · ตรวจ `g++ -std=c++17 -fsyntax-only Application.cpp` ผ่าน ยังไม่ได้รัน sim ยืนยันผล
